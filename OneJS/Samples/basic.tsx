import { h, render } from "preact"
import { Focusable } from "UnityEngine/UIElements"

const App = () => {
    return (
        <div>
          This is a test
        </div>
    )
}

render(<App />, document.body)